// #target: macos-aarch64
module test;

struct Abc
{
	int a;
	float[<3>] x;
	int b;
}

import std;

Abc x = { .a = 1, .x = (float[<3>])8, .b = 3 };
fn int main()
{
	Abc y = x;
	return 0;
}

/* #expect: test.ll

%Abc = type { i32, [3 x float], i32 }

@"$ct.test.Abc" = linkonce global %.introspect { i8 10, i64 0, ptr null, i64 20, i64 0, i64 3, [0 x i64] zeroinitializer }, align 8
@test.x = local_unnamed_addr global %Abc { i32 1, [3 x float] [float 8.000000e+00, float 8.000000e+00, float 8.000000e+00], i32 3 }, align 4

