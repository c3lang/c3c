// #target: macos-x64
module test;

fn void test()
{
	asm @align { nop; }
	asm @pure { nop; }
	asm("nop") @align;
	asm("nop") @pure;
}

/* #expect: test.ll

define void @test.test() #0 {
entry:
  call void asm sideeffect alignstack "nop \0A", "~{flags},~{dirflag},~{fspr}"()
  call void asm "nop \0A", "~{flags},~{dirflag},~{fspr}"()
  call void asm sideeffect alignstack "nop", "~{dirflag},~{fpsr},~{flags}"()
  call void asm "nop", "~{dirflag},~{fpsr},~{flags}"()
  ret void
}