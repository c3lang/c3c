// #target: macos-x64
module test;
import std::io;

fn void main()
{
    var $s1 = $stringify(1 + 2);
    char[*] s2 = $stringify($s1);
    char[] s3 = $s1;

    io::printfln("$s1 == %s", $s1);
    io::printfln("s2 == %s", &s2);
    io::printfln("s3 == %s", s3);
}
